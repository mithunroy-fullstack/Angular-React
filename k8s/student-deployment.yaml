apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-api
  labels:
    app: student-api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: student-api
  template:
    metadata:
      labels:
        app: student-api
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: student-api
          image: fullstackregistry-egdnarcrfxgaare7.azurecr.io/student-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ConnectionStrings__DefaultConnection
              value: "Server=<sql-server-host>;Database=StudentDB;User Id=<db-user>;Password=<db-password>;Encrypt=True;TrustServerCertificate=False;"
            - name: JwtSettings__Issuer
              value: "AuthService"
            - name: JwtSettings__Audience
              value: "AuthServiceUsers"
            - name: JwtSettings__Key
              value: "<your-jwt-secret-key>"
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: student-api-service
spec:
  type: ClusterIP
  selector:
    app: student-api
  ports:
    - port: 80
      targetPort: 8080