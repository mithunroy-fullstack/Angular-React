
apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-api  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: student-api
  template:
    metadata:
      labels:
        app: student-api
    spec:
      containers:
      - name: student-api
        image: fullstackregistry.azurecr.io/student-api:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: student-api-service  
spec:
  selector:
    app: student-api
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
